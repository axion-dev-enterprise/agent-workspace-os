# Installation templates

These files are source templates. The installer copies them into active local configuration only after the operator has supplied or approved the relevant values.

Keep generated configuration, credentials, runtime sessions, and local timeline data out of version control. Do not replace template placeholders in the upstream repository when preparing a distribution release.
