# Tool registry

`registry.json` is a portable allowlist. A tool is usable only when its capability is enabled in the local policy and its pre-flight conditions are satisfied. Tool metadata never grants authorization by itself.
